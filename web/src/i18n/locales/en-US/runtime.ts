const runtime = {
  'runtime.title': 'Punkdom frontend render error',
  'runtime.description': 'The crash reason was recorded in the browser console and localStorage.punkdom.runtime.logs.',
  'runtime.reload': 'Reload Page',
} as const

export default runtime
